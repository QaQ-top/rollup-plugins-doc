{
  "name": "start",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "yarn clean && rollup --config",
    "dev": "rollup --config --watch"
  },
  "devDependencies": {
    "@types/node": "^16.3.3",
    "rollup": "^2.53.2"
  }
}
